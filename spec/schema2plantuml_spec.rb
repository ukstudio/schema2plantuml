require "spec_helper"

describe Schema2plantuml do
  it "has a version number" do
    expect(Schema2plantuml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
